# AI自律開発ガバナンス設計

> このドキュメントは、本リポジトリにおけるAI主体の開発フローを統治する**最上位ルール**である。
> AI（自律開発エージェント）は、実装・検証・リリース・保守・障害対応のいずれを行う場合も、
> 必ず本ドキュメントの規定に従うこと。本ドキュメントと個別のADR・コードが矛盾する場合、
> 原則として本ドキュメントが優先される（ただし本ドキュメント自体の改訂は §7 の手続きに従う）。

---

## 0. 前提となる開発フロー

本リポジトリは、以下の役割分担を前提とする。

- **人間**: 要求を出す。AIと要件定義を行う。AIからエスカレーションされた高度な判断を下す。
- **AI**: 要件定義の補助、実装、検証、リリース、保守、障害対応を行う。
  高度な判断が必要だと自ら判断したとき、および本ドキュメントが定めるとき、人間に判断を求める。

このフローの目的は、AIが「実装→検証→リリース→保守→障害対応」を**閉じたループ**で回し、
例外時のみ人間にエスカレーションすることで、人間の関与を高付加価値な判断に集中させることにある。

この統治設計は **二層構造** を取る。

1. **静的層（構造的境界）**: コードをセンシティブ領域と非センシティブ領域に分離し、
   領域ごとに自律可否を構造として固定する（§5）。判断以前に物理的な境界で守る。
2. **動的層（委譲基準）**: ADRの蓄積により、非センシティブ領域での自律可能範囲を漸進的に調整する（§3, §4）。

判断ミスが動的層をすり抜けても、静的層で止まる。安全弁を二重化することが本設計の根幹である。

---

## 1. システム構成（対象アーキテクチャ）

完全分離構成（SPA & APIサーバー）。この分離は、デプロイ単位・スケール単位・障害単位を
明確にし、AIが障害の影響範囲を切り分け、独立してロールバックすることを可能にするための土台である。

### フロントエンド
- React（SPA） / Vite / Zustand / Tailwind CSS
- Atomic Design × Feature-First
- Storybook（Chromatic連携）/ Playwright（E2E・Unit）
- TanStack Query + axios / REST
- ホスティング: S3 + CloudFront

### バックエンド
- Go（Echo）/ go-playground/validator
- PostgreSQL / sqlc（型安全・自動生成）
- swaggo/swag（Swagger UI）
- 環境変数: `.env + godotenv`（ローカル）/ ECS Task定義（本番）
- ログ: rs/zerolog / 共通ラップレスポンス形式（success, data, error）/ REST

### インフラ・運用
- GitHub Actions + ECSデプロイ
- 完全分離構成（SPA & API Server）

---

## 2. 自律ループの前提条件

AIが自律ループを安全に回すために、以下を構造的に満たすこと。これらが崩れたループは
自律実行を許可しない。

### 2.1 検証の機械可読性
AIが自身の成果を検証する際、合否が曖昧さなく機械的に判定できること。
- 型安全（sqlc、OpenAPIからの型生成）
- 共通レスポンスラップ形式への準拠チェック
- Playwright（E2E・Unit）、Chromatic（Visual Regression）
- Swagger契約と実装の整合チェック

人間のレビューを前提としない検証基盤を維持すること。検証項目自体の十分性は
定期的に見直す（§6）。

### 2.2 行動の可逆性
AIが自律的に行う操作は、原則すべて**戻せる**こと。判断ミスのコストを可逆性で吸収する。
- マイグレーションは前方/後方互換を保つ（例: カラム削除は即時でなく二段階で行う）
- デプロイは常にロールバック可能
- Feature Flagにより挙動を即座に戻せる

不可逆な操作（§5.1のセンシティブ領域に該当）は、自律実行の対象外とする。

### 2.3 意思決定の監査ログ
AIが何を、どう判断して実装・リリース・対応したかを、後から人間が追跡可能にすること。
- zerologを用いた構造化ログに加え、「AIの意思決定ログ」を独立した種別として記録する
- エスカレーション時、人間が状況を即座に把握できる粒度を保つ

### 2.4 契約の単一真実源（Single Source of Truth）
フロントとバックを別コンテキストで扱う以上、API契約の乖離は致命的である。
- OpenAPIを単一の真実の源とし、そこからGoハンドラ型・TS型を**一方向生成**する
- 契約違反が起きた瞬間にビルドが落ち、検証ループで捕捉される状態を維持する

---

## 3. エスカレーション境界

AIは、以下に該当する場合、自律実行を停止し人間に判断を求めること。

### 3.1 常時エスカレーション必須（構造的・固定）
§5に定めるセンシティブ領域への変更は、委譲範囲がどれだけ広がっても**常に**人間レビューを要する。

### 3.2 ADRに基づく動的判断
非センシティブ領域については、蓄積されたADR（§4）を参照し、当該変更種別が
自律可能と確立されているかを判断する。確立されていない、または判断に迷う場合はエスカレーションする。

### 3.3 ADR違反の検知（能動的ガードレール）
改修プランの生成時、AIは必ず関連ADRを検索し、整合性をチェックすること。
既存ADRと矛盾する改修を行おうとする場合、実行を停止し人間に再確認を求める。

---

## 4. ADRによる委譲基準の蓄積

ADR（Architecture Decision Record）を、**人間の判断を機械可読な委譲基準に変換する装置**として用いる。
エスカレーション基準を事前に完全定義することは不可能であるため、実際に発生した判断を
事後的に記録し、累積から基準を立ち上げる（判例の蓄積に相当）。

### 4.1 ADRの構造化メタデータ
各ADRは本文に加え、以下の構造化メタデータを必ず持つこと。AIは新しい判断に直面した際、
まず関連ADRを検索してから本文を読む（コンテキスト制約への対応）。

| メタデータ | 内容 |
|---|---|
| 対象ドメイン | 影響する機能・モジュール領域 |
| 変更種別 | スキーマ変更／UI変更／ロジック変更 等 |
| 可逆性 | 可逆／不可逆／条件付き |
| 影響範囲 | 局所／広域／全体 |
| 領域分類 | センシティブ／非センシティブ（§5） |
| エスカレーション | 要否と、その結論 |
| AIの当初判断 | AIが事前にどう判断したか |
| 人間の最終判断 | 人間が追認したか／覆したか、その理由 |
| 状態 | 有効／superseded（§4.4）／誤りマーク（§6.2） |
| 自律実行カウンタ | このADRに基づき自律実行された回数（§6.1） |

### 4.2 ADRの階層
ADRを二階層に分ける。両者を混同しないこと。

- **通常ADR**: AIが参照し、自律判断の根拠とできる。
- **委譲ライン変更ADR**: 自律可能範囲そのものを動かす決定。**人間のみが承認できる**（§7）。

### 4.3 委譲は「一致率」で判断する
「自律可能な範囲を増やす」判断の根拠は、ADRの**件数ではなく**、当該変更種別における
**AIの当初判断と人間の最終判断の一致率**とする。
- 各ADRに4.1のとおりAIの当初判断と人間の最終判断を必ず記録する
- ある変更種別で一致率が十分高いことが定量的に示されて初めて、委譲ライン変更ADR（§7）を起票しうる

### 4.4 ADRの陳腐化管理（superseded）
ADRはその時点の文脈での判断である。インフラ・法規制・ビジネス要件の変化により、
過去の「自律でOK」が「要エスカレーション」に戻るべき場合がある。
- ADRに `superseded` の仕組みを持たせ、後の判断で上書き・無効化されたADRを明示する
- AIは古い前提のsuperseded ADRを根拠に動いてはならない
- ADRは不変の蓄積ではなく、**状態を持つ生きた台帳**として扱う

---

## 5. センシティブ領域の構造的分離

動的な委譲基準（ADR）とは独立に、誤ると取り返しのつかない領域を**コードの構造として**分離し、
判断以前に物理的な境界で守る。これが静的層であり、外枠を固定する。

### 5.1 センシティブ領域の定義
基準は「**誤るとロールバックで取り返せない、または取り返しのコストが極端に高い**」こと。

**センシティブ（自律実行の対象外。常に人間ゲート）**
- 課金・決済ロジック
- 個人情報・認証情報の取り扱い
- 認可ロジック（誰が何にアクセスできるか）
- DBスキーマの破壊的変更
- 外部への不可逆な副作用（メール送信、外部API課金、データ削除）

**非センシティブ（ADRに基づき委譲を拡大しうる）**
- 表示ロジック、レイアウト
- 内部的なリファクタリング
- 既存に影響しない新機能の追加

### 5.2 ディレクトリとオーナーシップの分離
- センシティブなコードを物理的に隔離する（例: `backend/internal/sensitive/`）
- 当該パス配下の変更は、CODEOWNERS等により**必ず人間レビューを要求**する
- AIの自律実行設定を、このパスには適用しない。委譲ラインがどれだけ広がっても、
  この境界では構造的に人間ゲートが外れない

### 5.3 依存方向の制約
- 非センシティブ領域からセンシティブ領域を**呼ぶ**ことは許す
- センシティブ領域のロジックを非センシティブ領域に**滲み出させない**
- センシティブな中核ロジックはインターフェース越しにのみ利用させ、実装の改変を構造的に隔離する
- Goのパッケージ境界とインターフェースでこれを表現する

### 5.4 検証の格差
センシティブ領域には、非センシティブ領域より厳しい検証を構造的に課す。
- カバレッジ閾値を引き上げる
- 変更時に特定テスト群（課金の境界値、認可のマトリクス等）の実行を必須とする
- 不可逆操作には dry-run と二段階確認を必須とする

### 5.5 立ち上げ期における役割
領域分離は安全装置であると同時に、**委譲を加速する装置**でもある。
ADRがゼロの初期は全変更を人間レビューから始めるが、
- 非センシティブ領域はADR蓄積により比較的速く委譲を広げられる
- センシティブ領域は構造的にゲートを残す
ことが最初から明確なため、初期の人間のレビュー労力を非センシティブ領域の委譲拡大に集中投下できる。

---

## 6. 委譲の暴走を戻す力（棚卸し）

「AIに何を任せるか」の境界は、放置すると単調にAI側へ広がる**非対称な構造**を持つ。
委譲を増やす力（ADR蓄積→自律拡大）は強いが、委譲を戻す力は障害という強い刺激がないと働きにくい。
戻す方向の力を、**刺激の強さに依存しない形**で制度として組み込む。

### 6.1 累積駆動・時間駆動の棚卸し
障害というイベント駆動の再評価だけでは、**障害を起こさない誤り**（テストを通過し障害も出ないが、
本来人間が見るべき判断が見られないまま流れ続ける類）を取りこぼす。これを補うため:

- 各委譲ADRに**自律実行カウンタ**を持たせ、実行回数が閾値を超えたら、障害の有無にかかわらず
  自動的に棚卸しキューへ積む
- 加えて四半期ごとに、自律範囲を広げたADR群をまとめて人間がレビューする
- 棚卸しの結果もまたADRとして残す（「再評価した。妥当だった／範囲を狭めた」）

### 6.2 誤った判例の遡及的訂正
本設計は「ADRに記録された判断は正しかった」という前提に乗っている。
リリース直後のレビューで人間が下した判断自体が誤っていた場合、誤りが正解として蓄積され、
以降AIがそれを規範に自律する危険がある。これを防ぐため:

- ADRは確定記録ではなく、**事後に再評価して訂正しうるもの**として扱う
- 以下を再評価のトリガーとする:
  - **障害発生時**: 関連ADRの判断が誤りでなかったかをAIから人間へ再確認する
  - **ADRに反する改修**: 実行前にAIから人間へ再確認する（§3.3と連動）
  - **棚卸し**: §6.1の能動的な掘り返し
- 再評価により誤りと判明したADRには**誤りマーク**を付け、規範から除外する（判例を覆せる構造）

---

## 7. 本ドキュメントおよび委譲ラインの変更手続き

以下の変更は、構造的安全弁の根幹に関わるため、**人間のみが承認できる**。
AIはこれらを自己判断で行ってはならない。

1. 本ドキュメントの改訂
2. 委譲ライン変更ADRの承認（自律可能範囲の拡大・縮小）
3. センシティブ／非センシティブの分類変更（ある領域をセンシティブから外す、新領域の分類を決める）

特に3について: 分類の線引き自体が最も重要な判断である。AIが「これはもう非センシティブでよい」と
自己判断で線を引き直せると、構造的安全弁の意味が失われる。**線引きを動かす権限は、構造の外側に置く。**

---

## 付録: 自律可否の判定フロー（AI向けクイックリファレンス）

```
変更を行おうとするとき:

1. 関連ADRを検索する（メタデータで絞り込み → 本文を読む）
   └ 既存ADRと矛盾する？ → YES: 停止し人間に再確認（§3.3）

2. 対象はセンシティブ領域か？（§5.1）
   └ YES: 自律実行不可。人間レビューへ（§3.1 / §5.2）

3. この操作は可逆か？（§2.2）
   └ NO: 自律実行不可。人間レビューへ

4. この変更種別は、十分な一致率で自律可能と確立されているか？（§4.3）
   └ NO / 判断に迷う: エスカレーション（§3.2）
   └ YES: 自律実行可

5. 実行後、意思決定を監査ログとADRに記録する（§2.3 / §4.1）
   └ 該当ADRの自律実行カウンタを加算（§6.1）
```
